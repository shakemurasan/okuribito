require 'spec_helper'

describe Okuribito do
  it 'has a version number' do
    expect(Okuribito::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
